<UserControl x:Class="MGF.DevConsole.Desktop.Modules.Jobs.Views.JobsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Jobs created in the last 24 hours" FontWeight="SemiBold" />
            <TextBlock Text="{Binding ConnectionStatusLine}" />
            <TextBlock Text="{Binding LastError}" TextWrapping="Wrap" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding Jobs}"
                      SelectedItem="{Binding SelectedJob, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      Margin="0,0,8,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt}" />
                    <DataGridTextColumn Header="Status" Binding="{Binding StatusKey}" />
                    <DataGridTextColumn Header="Type" Binding="{Binding JobTypeKey}" />
                    <DataGridTextColumn Header="Attempts" Binding="{Binding AttemptCount}" />
                    <DataGridTextColumn Header="Locked Until" Binding="{Binding LockedUntil}" />
                </DataGrid.Columns>
            </DataGrid>

            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Job Detail" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBlock Text="{Binding DetailError}" TextWrapping="Wrap" />

                    <TextBlock>
                        <Run Text="JobId: " FontWeight="SemiBold" />
                        <Run Text="{Binding DetailJobId, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Status: " FontWeight="SemiBold" />
                        <Run Text="{Binding DetailStatusKey, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Type: " FontWeight="SemiBold" />
                        <Run Text="{Binding DetailJobTypeKey, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Attempts: " FontWeight="SemiBold" />
                        <Run Text="{Binding DetailAttemptCount, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Created At: " FontWeight="SemiBold" />
                        <Run Text="{Binding DetailCreatedAt, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Run After: " FontWeight="SemiBold" />
                        <Run Text="{Binding DetailRunAfter, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Started At: " FontWeight="SemiBold" />
                        <Run Text="{Binding DetailStartedAt, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Finished At: " FontWeight="SemiBold" />
                        <Run Text="{Binding DetailFinishedAt, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Locked Until: " FontWeight="SemiBold" />
                        <Run Text="{Binding DetailLockedUntil, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Last Error: " FontWeight="SemiBold" />
                        <Run Text="{Binding DetailLastError, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Entity Type: " FontWeight="SemiBold" />
                        <Run Text="{Binding DetailEntityTypeKey, Mode=OneWay}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Entity Key: " FontWeight="SemiBold" />
                        <Run Text="{Binding DetailEntityKey, Mode=OneWay}" />
                    </TextBlock>

                    <CheckBox Content="Show payload" IsChecked="{Binding ShowPayload}" Margin="0,8,0,4" />
                    <TextBlock Text="Payload" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding JobDetailJson, Mode=OneWay}" TextWrapping="Wrap" />
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
